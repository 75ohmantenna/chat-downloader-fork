[pytest]
markers =
    network: tests that require external network access (not run in default CI)
    slow: slower tests (optional)
    unit: unit tests (optional)
filterwarnings =
    error::DeprecationWarning
    error::PendingDeprecationWarning
